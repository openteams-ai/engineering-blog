[project]
name = "engineering-blog"
version = "0.1.0"
description = "OpenTeams engineering blog publishing tools"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "markdown>=3.8",
    "pydantic>=2.0",
    "python-dotenv>=1.1",
    "pyyaml>=6.0",
    "requests>=2.32",
]
